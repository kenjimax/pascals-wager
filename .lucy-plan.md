# Plan: Build complete Pascal's Wager decision-theory instrument

## Assumptions
- Fresh Next.js 14 scaffold with all deps installed (augmented-ui, tremor, recharts, lz-string, lucide-react, vitest)
- Branch feat/pascals-wager-full-build exists, one scaffold commit
- PLAN.md is authoritative spec; Lucy CSS/Tailwind refs in reference/
- Daniel's prototype in docs/original-prototype.html
- No backend needed; entirely client-side

## Acceptance Criteria
1. `npm test` passes with comprehensive coverage of wager engine: extended-real arithmetic, all four decision rules, infinity tiebreak, sensitivity breakpoints, URL round trips, edge cases (zero credence, all-zero weights, indeterminate, ties)
2. `npm run typecheck` exits 0
3. `npm run lint` exits 0  
4. `npm run build` exits 0 (clean Next.js static build, no runtime backend)
5. Single-page app loads with default Pascalian binary scenario, result visible immediately
6. Four decision rules displayed with precise definitions, disagreement surfaced
7. Sensitivity analysis shows exact break-even intervals (not single thresholds)
8. Diverging heatmap with symbolic infinity badges, EU bars with extended-value lane
9. CP2077 visual language: Rajdhani + Share Tech Mono fonts, glitch title, scanlines, circuit grid, neon glow, augmented-ui panels
10. prefers-reduced-motion gates animations; keyboard-operable matrix; aria labels
11. Compressed URL state via lz-string + localStorage autosave; share button works
12. Five teaching presets precisely labeled per spec section 7
13. Limits panel reachable from persistent disclaimer chip
14. Daniel Eaton credited in README, prototype in docs/

## Approach
1. Write decision engine (src/lib/wager.ts) - pure, no UI imports
2. Write exhaustive engine tests (src/lib/__tests__/wager.test.ts)  
3. Write URL state codec (src/lib/state.ts)
4. Write presets (src/lib/presets.ts)
5. Port CP2077 visual language (tailwind.config.ts, globals.css, fonts)
6. Build UI components bottom-up: panels, inputs, results, visualizations
7. Wire main page with progressive disclosure layout
8. Verify all acceptance criteria pass
9. Update README, commit, open PR

## Progress
- [x] Step 1: Decision engine (wager.ts with extended reals, 4 rules, sensitivity, templates)
- [x] Step 2: Engine tests (93 tests, all passing)
- [x] Step 3: URL state codec (lz-string compression + localStorage)
- [x] Step 4: Presets (5 teaching presets per spec)
- [x] Step 5: Visual language (tailwind config, globals.css, Rajdhani + Share Tech Mono fonts)
- [x] Step 6: UI components (10 components: GlitchTitle, DisclaimerChip, LimitsPanel, WorldviewCards, CredenceEditor, PayoffMatrix, ResultCard, DecisionRulesPanel, HeatmapViz, EUBarsViz, SensitivityViz, SimplexViz, PresetSelector, WagerApp)
- [x] Step 7: Main page wiring (progressive disclosure, desktop 2-col, mobile tabbed)
- [x] Step 8: Verification (lint 0 errors, typecheck 0 errors, 93 tests pass, build succeeds)
- [ ] Step 9: Commit + PR

## Decision Log

## Surprises & Discoveries
